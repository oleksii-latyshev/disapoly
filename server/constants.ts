/** Grace period before a disconnected player's turn is auto-skipped. */
export const AUTO_SKIP_MS = 30_000

/** Grace period before an emptied-out match is abandoned back to the lobby. */
export const ABANDON_MS = 60_000

/** Storage key under which the whole room state is persisted. */
export const STORAGE_KEY = "room"
