# Copy to .env.production and set your deployed Worker host (no protocol).
# Used at build time by `bun run build`. Local dev (`bun run dev`) falls back
# to 127.0.0.1:8787 automatically, so you don't need this file for local work.
VITE_PARTYKIT_HOST=your-worker.your-subdomain.workers.dev
